import { Box, styled } from '@mui/material';

const DashboardInfoContainer = styled(Box)(() => ({
    flex: 4,
    display: 'flex',
    flexDirection: 'column',
    gap: '20px',
    margin: '50px auto',
    padding: '0 50px'
}));

export default DashboardInfoContainer;
